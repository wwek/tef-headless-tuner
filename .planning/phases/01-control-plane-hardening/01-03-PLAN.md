---
phase: 01-control-plane-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - main/web_server.c
  - main/main.c
  - main/wifi_manager.c
  - README.md
  - README.zh-CN.md
autonomous: true
requirements:
  - PLAT-03
  - CTRL-04
user_setup: []
must_haves:
  truths:
    - Runtime diagnostics make it obvious which transport address/mode the device expects users to use.
    - Final control-plane-facing docs and logs do not mislead users about Wi-Fi mode or supported access paths.
    - Phase 1 leaves the project easier to operate and debug before hardware arrives.
  artifacts:
    - main/web_server.c
    - main/main.c
    - main/wifi_manager.c
    - README.md
    - README.zh-CN.md
  key_links:
    - startup logs and Wi-Fi/web diagnostics align with the actual runtime mode and access path
    - user-facing docs reflect the control-plane behavior actually implemented by Phase 1
---

<objective>
Finish Phase 1 with the remaining parity and diagnostics cleanup needed to make the control plane understandable and supportable.

Purpose: Reduce operator confusion and close the last transport-facing rough edges after controller and XDR hardening.
Output: Better startup/runtime diagnostics plus docs aligned to actual current control-plane behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-control-plane-hardening/01-RESEARCH.md
@.planning/phases/01-control-plane-hardening/01-01-PLAN.md
@.planning/phases/01-control-plane-hardening/01-02-PLAN.md
@main/main.c
@main/wifi_manager.c
@main/web_server.c
@README.md
@README.zh-CN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve startup and runtime diagnostics for control-plane visibility</name>
  <files>main/main.c, main/wifi_manager.c, main/web_server.c</files>
  <read_first>main/main.c, main/wifi_manager.c, main/web_server.c, .planning/phases/01-control-plane-hardening/01-RESEARCH.md</read_first>
  <action>Refine logs that tell operators how the device should currently be reached and what mode it is in. Keep the logs concise but explicit enough to answer common bring-up questions such as which AP SSID is active, whether the device is in AP or STA mode, and whether HTTP requests are reaching the expected handler paths. Do not turn this into a large diagnostics framework; just make the most important startup and control-plane runtime signals obvious.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - main/main.c logs startup transport/runtime summary information relevant to Wi-Fi access
    - main/wifi_manager.c logs the resulting Wi-Fi mode and AP/connection state after initialization
    - main/web_server.c contains explicit request logging for the main control and Wi-Fi entry pages or equivalent access-path diagnostics
    - idf.py build exits 0
  </acceptance_criteria>
  <done>Operators can tell how to reach the current control plane from logs alone.</done>
</task>

<task type="auto">
  <name>Task 2: Align Wi-Fi and web-facing behavior with actual current implementation</name>
  <files>main/web_server.c, README.md, README.zh-CN.md</files>
  <read_first>main/web_server.c, main/html/index.html, main/html/wifi.html, README.md, README.zh-CN.md</read_first>
  <action>Review the remaining web-facing behavior and documentation for mismatches between what the firmware really does and what users are likely to assume. Adjust docs or small server-side behavior where Phase 1 work made the current access path clearer. Keep scope focused on accurate control-plane behavior, not broad content rewriting.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - README.md describes current XDR/Wi-Fi control behavior in terms that match implemented behavior
    - README.zh-CN.md reflects the same current facts as README.md
    - main/web_server.c does not silently hide obvious web-control failure conditions where an explicit error or log is feasible
    - idf.py build exits 0
  </acceptance_criteria>
  <done>Docs and web-facing control expectations match implemented behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] idf.py build
- [ ] grep -n "Startup config:" main/main.c confirms startup summary logging exists
- [ ] grep -n "Wi-Fi ready:" main/wifi_manager.c confirms Wi-Fi result logging exists
- [ ] grep -n "HTTP GET /wifi\\|HTTP GET / ->" main/web_server.c confirms access-path request logging exists
- [ ] grep -n "XDR-GTK Server\\|Wi-Fi" README.md README.zh-CN.md confirms docs still include control-plane guidance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Logs and docs make the current control-plane access path understandable
- Phase 1 ends with transport parity and diagnostics in a supportable state
</success_criteria>

<output>
After completion, create `.planning/phases/01-control-plane-hardening/01-03-SUMMARY.md`
</output>
