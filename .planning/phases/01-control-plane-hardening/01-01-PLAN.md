---
phase: 01-control-plane-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/tuner_frontend/tuner_controller.c
  - components/tuner_frontend/tuner_controller.h
  - main/cmd_handler.c
  - main/web_server.c
autonomous: true
requirements:
  - PLAT-03
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
user_setup: []
must_haves:
  truths:
    - CDC and Web use the same tuner-controller-backed semantics for tune, seek, mute, volume, and band changes.
    - Invalid tune/seek/control inputs return explicit errors instead of silently doing nothing.
    - Shared controller state is stable enough that later XDR work can rely on it.
  artifacts:
    - components/tuner_frontend/tuner_controller.c
    - components/tuner_frontend/tuner_controller.h
    - main/cmd_handler.c
    - main/web_server.c
  key_links:
    - cmd_handler and web_server call tuner_controller entry points for their core radio-control operations.
    - transport-side validation matches tuner-controller-supported frequency/band behavior.
---

<objective>
Stabilize the controller contract used by CDC and Web before touching deeper XDR protocol details.

Purpose: Make transport-facing control semantics predictable and shared.
Output: Controller and transport adapter updates that eliminate avoidable drift in tune/seek/mute/band behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-control-plane-hardening/01-RESEARCH.md
@AGENTS.md
@components/tuner_frontend/tuner_controller.h
@components/tuner_frontend/tuner_controller.c
@main/cmd_handler.c
@main/web_server.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align controller state and transport-facing semantics</name>
  <files>components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c</files>
  <read_first>components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-control-plane-hardening/01-RESEARCH.md</read_first>
  <action>Audit tune, seek, mute, band, and state-query paths in tuner_controller. Tighten any remaining ambiguous transport-facing behavior so callers can rely on a single contract. Keep the public controller interface explicit and minimal. Do not add unrelated hardware-bring-up logic. If a state or error outcome is transport-visible, make it deterministic at the controller boundary instead of leaving adapters to guess.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - components/tuner_frontend/tuner_controller.h exposes the controller entry points needed by CDC and Web without duplicate transport-specific variants for the same core operation
    - components/tuner_frontend/tuner_controller.c contains explicit handling for controller-visible failure paths instead of silent fallthrough in tune/seek/mute-related operations
    - idf.py build exits 0
  </acceptance_criteria>
  <done>Controller contract is stable and build-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize CDC command behavior against the controller contract</name>
  <files>main/cmd_handler.c</files>
  <read_first>main/cmd_handler.c, components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c, .planning/phases/01-control-plane-hardening/01-RESEARCH.md</read_first>
  <action>Review CDC command parsing and responses so tune, seek, seek stop, mute, volume, power, and status operations line up with the shared controller behavior. Keep command help and runtime responses explicit. Where invalid arguments are possible, return actionable error text rather than generic or silent failure.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - main/cmd_handler.c contains explicit error responses for invalid command arguments in tune/seek/band/volume style operations
    - main/cmd_handler.c routes core radio-control commands through tuner_controller APIs rather than direct chip-driver calls
    - idf.py build exits 0
  </acceptance_criteria>
  <done>CDC command behavior is aligned with the controller contract.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize Web API behavior against the same controller contract</name>
  <files>main/web_server.c</files>
  <read_first>main/web_server.c, components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c, main/html/index.html, main/html/wifi.html</read_first>
  <action>Review REST handlers and SSE-facing state generation so Web tune/seek/mute/volume/status behavior matches the CDC/controller contract. Keep request validation concrete: unsupported bands, invalid directions, and malformed JSON must return explicit HTTP errors. Do not expand scope into broader UI redesign or Wi-Fi captive-portal feature work here.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - main/web_server.c validates malformed or unsupported tune/seek inputs with explicit HTTP 400 or 500 responses
    - main/web_server.c uses tuner_controller-backed operations for core radio actions
    - idf.py build exits 0
  </acceptance_criteria>
  <done>Web API semantics match the shared controller contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] idf.py build
- [ ] grep -n "tuner_controller_" main/cmd_handler.c main/web_server.c confirms controller-backed control paths remain in place
- [ ] grep -n "ERROR" main/cmd_handler.c and grep -n "HTTPD_400_BAD_REQUEST\\|HTTPD_500_INTERNAL_SERVER_ERROR" main/web_server.c show explicit failure handling remains present
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- CDC and Web rely on the same controller-backed control semantics
- Invalid transport inputs no longer rely on silent behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-control-plane-hardening/01-01-SUMMARY.md`
</output>
