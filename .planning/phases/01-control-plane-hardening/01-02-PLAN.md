---
phase: 01-control-plane-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - main/xdr_server.c
  - main/xdr_server.h
  - components/tuner_frontend/tuner_controller.h
  - components/tuner_frontend/tuner_controller.c
autonomous: true
requirements:
  - XDR-01
  - XDR-02
user_setup: []
must_haves:
  truths:
    - XDR clients can initialize, authenticate, and receive protocol-compatible state, seek, and raw RDS updates.
    - XDR command handling remains stable under fragmented input and buffered output conditions expected by current scope.
    - XDR protocol behavior stays aligned with the shared tuner controller instead of implementing divergent transport logic.
  artifacts:
    - main/xdr_server.c
    - main/xdr_server.h
  key_links:
    - xdr_server uses tuner_controller as the canonical control/state source.
    - XDR init and state frames reflect the controller-backed runtime settings.
---

<objective>
Finish hardening the XDR transport so it behaves like a reliable protocol adapter instead of a special-case control path.

Purpose: Deliver protocol-correct XDR behavior for current supported commands and state push flows.
Output: A hardened XDR server with stable init/state/RDS/seek behavior and clearer protocol boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-control-plane-hardening/01-RESEARCH.md
@.planning/phases/01-control-plane-hardening/01-01-PLAN.md
@main/xdr_server.h
@main/xdr_server.c
@components/tuner_frontend/tuner_controller.h
@components/tuner_frontend/tuner_controller.c
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten XDR init and state-frame correctness</name>
  <files>main/xdr_server.c, main/xdr_server.h</files>
  <read_first>main/xdr_server.c, main/xdr_server.h, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-control-plane-hardening/01-RESEARCH.md</read_first>
  <action>Review XDR handshake, init frame generation, periodic status push, seek completion, and raw RDS output paths. Ensure the supported command set returns protocol-consistent acknowledgments and that init/state frames are sourced from canonical controller/runtime state. Keep fixes focused on current supported commands rather than broad new protocol coverage.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - main/xdr_server.c contains explicit init-state helper calls for the XDR transport rather than ad hoc inline frame building in the `x` path
    - main/xdr_server.c contains explicit protocol handling for seek completion and raw RDS frame output
    - idf.py build exits 0
  </acceptance_criteria>
  <done>XDR init and state frames are internally consistent and build-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Harden XDR buffering and connection lifecycle handling</name>
  <files>main/xdr_server.c</files>
  <read_first>main/xdr_server.c, .planning/phases/01-control-plane-hardening/01-RESEARCH.md, .planning/phases/01-control-plane-hardening/01-01-PLAN.md</read_first>
  <action>Review connection lifetime, partial input assembly, queued output flushing, and stop/restart behavior in the XDR server. Keep framing safe under partial TCP reads and buffered output pressure. Avoid introducing transport-wide blocking behavior that can stall unrelated clients. Where best-effort push semantics are acceptable (status/RDS), keep them clearly separate from required command acknowledgments.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - main/xdr_server.c contains per-client receive buffering rather than parsing only the latest recv chunk
    - main/xdr_server.c distinguishes between required command replies and best-effort pushed state output
    - main/xdr_server.c contains explicit lifecycle cleanup for scan ownership or queued client state on disconnect/stop
    - idf.py build exits 0
  </acceptance_criteria>
  <done>XDR buffering and client-lifecycle behavior are hardened for current scope.</done>
</task>

<task type="auto">
  <name>Task 3: Keep XDR aligned with controller-owned state and settings</name>
  <files>components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c, main/xdr_server.c</files>
  <read_first>components/tuner_frontend/tuner_controller.h, components/tuner_frontend/tuner_controller.c, main/xdr_server.c</read_first>
  <action>Make any minimal controller-side adjustments required so XDR init/state helpers can read canonical runtime settings without re-deriving them inside the transport layer. Keep ownership clear: controller owns radio/runtime state, XDR translates it into protocol frames.</action>
  <verify>idf.py build</verify>
  <acceptance_criteria>
    - tuner_controller.h and tuner_controller.c expose only the state/settings access needed by xdr_server for supported protocol behavior
    - main/xdr_server.c reads controller-owned state/settings rather than duplicating persistent transport-local interpretations where avoidable
    - idf.py build exits 0
  </acceptance_criteria>
  <done>XDR transport uses controller-owned state instead of drifting into a parallel state model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] idf.py build
- [ ] grep -n "case 'x'" main/xdr_server.c shows explicit init-state handling
- [ ] grep -n "queue_client_\\|flush_client_" main/xdr_server.c shows buffered I/O paths remain present
- [ ] grep -n "R%04X%04X%04X%02X" main/xdr_server.c confirms raw RDS frame output path remains explicit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- XDR init/state/seek/RDS behavior is protocol-consistent
- XDR does not maintain unnecessary transport-private state that diverges from the controller
</success_criteria>

<output>
After completion, create `.planning/phases/01-control-plane-hardening/01-02-SUMMARY.md`
</output>
